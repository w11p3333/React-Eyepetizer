import styled from 'styled-components'
import { detail } from '@/style'

export default styled.article`
  position: relative;
  background-image: url(${detail.bgImg});
  background-position: 50%;
  background-size: 100% 100%;
`
