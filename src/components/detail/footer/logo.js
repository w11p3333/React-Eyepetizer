import styled from 'styled-components'
import { detail } from '@/style'

export default styled.figure`
    background-size: cover;
    margin: 0 auto 40px;
    background-image: url(${detail.logoImg});
    height: 94px;
    width: 90px;
`
